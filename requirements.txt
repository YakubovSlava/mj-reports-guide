# Пакеты, доступные в окружении платформы MJ.
# Скрипты отчётов могут использовать ТОЛЬКО эти библиотеки.
# Версии зафиксированы — не устанавливай другие версии локально,
# иначе отчёт может вести себя по-разному на платформе.

# ── Данные ────────────────────────────────────────────────────────────────────
pandas==2.3.3
numpy==2.0.2
openpyxl==3.1.5          # чтение/запись Excel (.xlsx)
scipy==1.13.1             # статистика, агрегация

# ── Визуализация ──────────────────────────────────────────────────────────────
plotly==6.8.0             # интерактивные графики → fig.to_html(include_plotlyjs='cdn')
matplotlib==3.9.4         # статичные графики → вывод как base64 SVG/PNG

# ── Утилиты ───────────────────────────────────────────────────────────────────
python-dateutil==2.9.0.post0   # удобный парсинг дат
pytz==2026.2                   # временные зоны

# ── Стандартная библиотека Python (не требует установки, всегда доступна) ─────
# csv, json, html, datetime, collections, itertools, math, statistics,
# pathlib, re, io, sys, os, tempfile, functools, operator, typing
